# tuples = ordered/sorted data structure which is immutable
myTuple = (1,)
print(type(myTuple))

# tuples occupy fewer memory than lists
print(().__sizeof__())
print([].__sizeof__())

# use: define months of the year
months_of_the_year = ('Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec')
print(months_of_the_year)

 